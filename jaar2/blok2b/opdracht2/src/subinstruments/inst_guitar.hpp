#include "../instrument.hpp"

class InstGuitar: public Instrument{
  public:
    // --------------------- Constructor and Destructor --------------------- //
    InstGuitar();
    InstGuitar(std::string name);
    ~InstGuitar();

    // --------------------- Functions -------------------------------------- //
};
