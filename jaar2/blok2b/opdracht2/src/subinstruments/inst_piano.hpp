#include "../instrument.hpp"

class InstPiano: public Instrument{
  public:
    // --------------------- Constructor and Destructor --------------------- //
    InstPiano();
    InstPiano(std::string name);
    ~InstPiano();

    // --------------------- Functions -------------------------------------- //
};
